parameter
 ( nat   %append
 | unit  %record
 );
storage ((list nat), address %feather);
code { UNPAIR; CASE
        { DIP {UNPAIR}; CONS; PAIR; NIL operation; PAIR }
        { DROP; DUP; CDR;
          CONTRACT (pair unit (option nat)); ASSERT_SOME;
          NONE nat; UNIT; PAIR;
          DIP {AMOUNT}; TRANSFER_TOKENS;
          NIL operation; SWAP; CONS; PAIR;
        };
    };
